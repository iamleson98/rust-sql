
name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:

# Group name carries a generation suffix so a zombie run stuck in `queued`
# (e.g. waiting for a self-hosted runner that no longer exists) can never
# starve fresh runs: bump -v2 to -v3 etc. to move all future runs into a
# clean group. cancel-in-progress still supersedes RUNNING older runs.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-v3
  cancel-in-progress: true

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: "0"
  RUST_BACKTRACE: "1"

defaults:
  run:
    shell: bash

# ============================================================================
# SPEED POLICY (why this file looks the way it does)
#
# 1. Correctness jobs (test/clippy/oom/compat) build the DEV/TEST profile
#    with debuginfo OFF (CARGO_PROFILE_*_DEBUG=0). Release builds were the
#    #1 runtime sink: 45+ minutes per cold job. Debug+no-debuginfo compiles
#    3-5x faster and links far faster (especially on Windows).
#
# 2. `cargo test --lib --tests` skips the ~30 example binaries. Examples
#    are still covered by the clippy --all-targets jobs and by the
#    bench-gate jobs that build the 4 bench examples in release.
#
# 3. EVERY rust-cache step sets cache-on-failure: true. Previously a failed
#    job threw away its whole dependency build, so every retry paid a full
#    cold compile — the death spiral that made iterating on CI glacial.
#    Now even a red job warms the cache for the next push.
#
# 4. Only the bench gate keeps the real release profile (fat LTO,
#    codegen-units=1) — throughput fidelity is the whole point of the gate.
#
# 5. Tight job timeouts: a stuck job can no longer burn 45-60 minutes.
# ============================================================================

jobs:
  # ---------------------------------------------------------------- formatting
  fmt:
    name: rustfmt
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt --all --check

  # ------------------------------------------------------------------- linting
  clippy:
    name: clippy (${{ matrix.config }})
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        # The four configs the project keeps clippy-clean in:
        #   default / +sqlx / --no-default-features / --workspace (compat crate)
        config: [default, sqlx, no-default, workspace]
    env:
      CARGO_PROFILE_DEV_DEBUG: "0"
      CARGO_PROFILE_TEST_DEBUG: "0"
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: clippy-${{ matrix.config }}
          cache-on-failure: true
      - name: cargo clippy
        run: |
          case "${{ matrix.config }}" in
            default)     cargo clippy --all-targets -- -D warnings ;;
            sqlx)        cargo clippy --all-targets --features sqlx -- -D warnings ;;
            no-default)  cargo clippy --all-targets --no-default-features -- -D warnings ;;
            workspace)   cargo clippy --workspace --all-targets -- -D warnings ;;
          esac

  # --------------------------------------------------------------------- tests
  # Correctness in the fast dev profile (debuginfo off). LTO settings do not
  # apply to dev — that release-only env is intentionally gone.
  #
  # COST LAYOUT (private repo: Linux 1x, Windows 2x, macOS 10x minute
  # multiplier — the macOS multiplier is the budget killer):
  #   - Linux   : 3 parallel jobs (cheap, parallel = fast wall time) +
  #               doc-tests once.
  #   - Windows : ONE consolidated job, 3 configs sequentially sharing one
  #               target dir / cache (one checkout, one dep compile).
  #   - macOS   : same consolidation as Windows — at 10x this cuts the
  #               per-run macOS bill by a third versus a 3-job matrix.
  # Each consolidated job runs every config even if an earlier one failed,
  # and reports a per-config summary at the end.
  test:
    name: test (ubuntu-latest, ${{ matrix.config }})
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        config: [default, sqlx, no-default]
    env:
      CARGO_PROFILE_DEV_DEBUG: "0"
      CARGO_PROFILE_TEST_DEBUG: "0"
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: test-ubuntu-latest-${{ matrix.config }}
          cache-on-failure: true
      # Extension (.so plugin) tests: build C/C++/Rust plugins first
      # (Zig is optional). Missing binaries make those tests skip.
      - name: Build extension plugins
        run: bash tests/build_plugins.sh
      # Doc-tests run once (ubuntu, default features) instead of 9x
      # redundancy in the old plain `cargo test` matrix.
      - name: cargo test (${{ matrix.config }})
        run: |
          # NOTE: cargo forbids mixing --doc with --lib/--tests, so doc-tests
          # run as a separate invocation (default config only, once).
          case "${{ matrix.config }}" in
            default)
              cargo test --lib --tests
              cargo test --doc
              ;;
            sqlx)        cargo test --lib --tests --features sqlx ;;
            no-default)  cargo test --lib --tests --no-default-features ;;
          esac

  test-windows:
    name: test (windows-latest, all configs)
    runs-on: windows-latest
    timeout-minutes: 45
    env:
      CARGO_PROFILE_DEV_DEBUG: "0"
      CARGO_PROFILE_TEST_DEBUG: "0"
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: test-windows-latest
          cache-on-failure: true
      # cc is not reliably available in bash here; the plugin tests skip.
      - name: cargo test (default, sqlx, no-default)
        run: |
          FAIL=0
          echo "::group::cargo test (default)"
          cargo test --lib --tests || FAIL=1
          echo "::endgroup::"
          echo "::group::cargo test (sqlx)"
          cargo test --lib --tests --features sqlx || FAIL=1
          echo "::endgroup::"
          echo "::group::cargo test (no-default)"
          cargo test --lib --tests --no-default-features || FAIL=1
          echo "::endgroup::"
          if [ "$FAIL" -ne 0 ]; then
            echo "One or more feature configs failed on windows-latest."
            exit 1
          fi

  test-macos:
    name: test (macos-latest, all configs)
    runs-on: macos-latest
    timeout-minutes: 45
    env:
      CARGO_PROFILE_DEV_DEBUG: "0"
      CARGO_PROFILE_TEST_DEBUG: "0"
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: test-macos-latest
          cache-on-failure: true
      - name: Build extension plugins
        run: bash tests/build_plugins.sh
      - name: cargo test (default, sqlx, no-default)
        run: |
          FAIL=0
          echo "::group::cargo test (default)"
          cargo test --lib --tests || FAIL=1
          echo "::endgroup::"
          echo "::group::cargo test (sqlx)"
          cargo test --lib --tests --features sqlx || FAIL=1
          echo "::endgroup::"
          echo "::group::cargo test (no-default)"
          cargo test --lib --tests --no-default-features || FAIL=1
          echo "::endgroup::"
          if [ "$FAIL" -ne 0 ]; then
            echo "One or more feature configs failed on macos-latest."
            exit 1
          fi

  # OOM fault injection: 532 allocation-failure points, each verified to
  # leave the baseline intact (SQLite's memsys fault-injection equivalent).
  # Uses its own allocator, so it must build without mimalloc.
  test-oom:
    name: test (ubuntu, oom-injection)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      CARGO_PROFILE_DEV_DEBUG: "0"
      CARGO_PROFILE_TEST_DEBUG: "0"
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: test-oom
          cache-on-failure: true
      - run: cargo test --lib --tests --no-default-features --features oom-injection

  # The C-ABI compatibility layer (sqlite3_* symbols over the rustqlite
  # engine) — its own differential/ABI test suite.
  test-compat:
    name: test (ubuntu, compat ABI)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      CARGO_PROFILE_DEV_DEBUG: "0"
      CARGO_PROFILE_TEST_DEBUG: "0"
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: test-compat
          cache-on-failure: true
      - run: cargo test --lib --tests -p rustqlite-compat

  # ------------------------------------------------------------- BENCH GATE --
  # rustqlite must beat (or tie) SQLite on EVERY row, on EVERY OS.
  # Any feature losing throughput to SQLite fails the pipeline.
  bench-gate:
    name: bench-gate (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 50
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    env:
      # A row fails when rustqlite is this much slower than SQLite (percent).
      # 5 = the project's own tie band (wins AND ties pass). 0 = strict
      # every-row-must-win. Keep >=3 on shared CI runners.
      BENCH_TOLERANCE_PCT: "5"
      # Plain text only: the gate parses this job's stdout.
      CARGO_TERM_COLOR: never
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - uses: actions/setup-python@v7
        with:
          python-version: "3.x"
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: bench-${{ matrix.os }}
          cache-on-failure: true
      # NOTE: no LTO overrides here — throughput fidelity requires the real
      # release profile (lto=fat, codegen-units=1).

      - name: "Gate 1/4: bench_full_vs_sqlite (18 workloads, ops/s)"
        run: >
          python .github/scripts/bench_gate.py
          --parser bench_full_vs_sqlite
          --timeout 1800
          -- cargo run --release --example bench_full_vs_sqlite

      - name: "Gate 2/4: bench_compare (20 workloads, times)"
        run: >
          python .github/scripts/bench_gate.py
          --parser bench_compare
          --timeout 1800
          -- cargo run --release --example bench_compare

      - name: "Gate 3/4: criterion sqlite_comparison (8 paired scenarios)"
        run: >
          python .github/scripts/bench_gate.py
          --parser criterion_sqlite_comparison
          --attempts 2
          --timeout 1800
          -- cargo bench --bench sqlite_comparison -- --noplot --sample-size 20 --warm-up-time 1 --measurement-time 2

      - name: "Gate 4/4: bench_sqlx_native (sqlx driver vs sqlx-sqlite)"
        run: >
          python .github/scripts/bench_gate.py
          --parser bench_sqlx_native
          --timeout 1800
          -- cargo run --release --features sqlx --example bench_sqlx_native

      # Runs ONLY when a gate above failed: per-component cost breakdowns
      # (open/create/insert, pinned + unpinned) so perf losses can be
      # attributed from the CI runner itself instead of guessed.
      - name: Diagnose bench losses (on failure)
        if: failure()
        run: |
          echo "===== probe_point_shapes (rowid vs indexed vs gate-1 shape) ====="
          cargo run --release --example probe_point_shapes 2>&1 | grep -E "shape" || true
      - name: Diagnose bench losses (Linux only, extra probes)
        if: failure() && runner.os == 'Linux'
        run: |
          cargo build --release --example probe_mv_breakdown --example probe_open_cost --example probe_mv_insert 2>&1 | tail -1
          echo "===== probe_open_cost (open_in_memory vs SQLite) ====="
          ./target/release/examples/probe_open_cost
          echo "===== probe_mv_breakdown (unpinned) ====="
          ./target/release/examples/probe_mv_breakdown
          echo "===== probe_mv_breakdown (pinned, CPU 0) ====="
          taskset -c 0 ./target/release/examples/probe_mv_breakdown || true
          echo "===== probe_mv_insert (phase attribution) ====="
          ./target/release/examples/probe_mv_insert
          echo "===== bench_full_vs_sqlite (pinned, CPU 0) ====="
          taskset -c 0 ./target/release/examples/bench_full_vs_sqlite 2>&1 | grep -E "ratio:|REMAINING|HEADLINE" | head -40 || true

      - name: Upload bench results and logs
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: bench-results-${{ matrix.os }}
          path: target/bench-results/
          if-no-files-found: ignore

  # ------------------------------------------------------------ aggregate OK
  ci-ok:
    name: ci-ok (all gates green)
    needs: [fmt, clippy, test, test-windows, test-macos, test-oom, test-compat, bench-gate]
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Fail if any gate failed or was cancelled
        if: >
          contains(join(needs.*.result, ','), 'failure')
          || contains(join(needs.*.result, ','), 'cancelled')
        run: |
          echo "One or more required jobs failed:"
          echo "${{ join(needs.*.result, ',') }}"
          exit 1
      - name: All green
        run: echo "fmt + clippy + tests + beat-SQLite bench gates all green on Linux, Windows and macOS."
